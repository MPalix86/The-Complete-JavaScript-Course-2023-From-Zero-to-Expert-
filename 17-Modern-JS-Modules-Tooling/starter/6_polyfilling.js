/**
 * dopo aver attivato il building è arrivato il momento di configurare babel per il
 * polyfilling.
 * ricordiamo che questo processo traduci tutto il nuovo codice ES6 in codice compatibile con qualsiasi versione di js.
 *
 * la buona notizia è che parcel ha gia integrato babel e fa questo per noi , ovviamente noi possiamo
 * configurare babel e dire con estrema precisione quali browser vanno supportati o no , oppure possiamo far
 * fare tutto a parcel che configurera per noi babel !
 */
